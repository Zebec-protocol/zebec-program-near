{
  "name": "zebec",
  "version": "1.0.0",
  "license": "(MIT AND Apache-2.0)",
  "scripts": {
    "deploy": "cd contract && ./deploy.sh",
    "build": "npm run build:contract",
    "build:test": "npm run build:contract:test",
    "build:contract": "cd contract && ./build.sh",
    "build:contract:test": "cd contract && ./build_testnet.sh",
    "test": "npm run test:unit",
    "test:unit": "cd contract && cargo test --features testnet",
    "test:integration": "npm run build:contract:test && cd integration-tests && cargo run --example integration-tests \"../contract/target/wasm32-unknown-unknown/release/zebec.wasm\""
  },
  "devDependencies": {
    "near-cli": "^3.3.0"
  },
  "dependencies": {}
}
